# 安全策略

## 报告漏洞

请使用本仓库 **Security** 页的 **Report a vulnerability** 私密报告入口，不要在公开 Issue 中提交未修复漏洞、凭据、真实服务器地址、玩家数据或复现环境截图。

报告至少应包含：

- 受影响版本或提交；
- 可重复的最小步骤；
- 实际影响和已验证边界；
- 脱敏后的日志、请求或调用栈；
- 可行的缓解建议（如有）。

维护者确认问题前，请不要公开披露细节。普通功能缺陷和文档问题可直接提交 Issue。

## 支持范围

安全修复面向 `main` 最新版本。历史提交和自行修改的分支不承诺单独维护。

## 运行边界

- Portal 默认只监听 `127.0.0.1`；远程访问必须放在受控 HTTPS 反向代理后。
- 游戏数据库使用独立只读账号，禁止使用 root 或公网开放数据库端口。
- `.env.local`、`data/`、AI Key、数据库密码、SDK 密钥和发布私钥不得提交到仓库。
- AI 输出只能辅助研判，封停、冻结、扣除等处置必须核验原始证据并由授权人员确认。

更完整的威胁模型见 [docs/SECURITY_AND_LICENSING.md](docs/SECURITY_AND_LICENSING.md)。
